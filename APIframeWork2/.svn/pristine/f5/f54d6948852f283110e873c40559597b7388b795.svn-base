#pragma once
#include "Scene.h"

class CAnimationEditScene :
	public Scene
{
	friend class SceneManager;
	friend class CAniEditDlg;

private:
	class CAniEditDlg*					m_pAniEditDlg;
	POSITION							m_tStart;
	POSITION							m_tEnd;
	int									m_iSelect;
	POSITION							m_tMouseClickPos;

public:
	void SetStart(float x, float y)
	{
		m_tStart = POSITION(x, y);
	}

private:
	CAnimationEditScene();
	~CAnimationEditScene();

public:
	virtual bool Init();
	virtual void Input(float fDeltaTime);
	virtual int Update(float fDeltaTime);
	virtual void Render(HDC hDC, float fDeltaTime);

public:
	void EraseFrame();
	void EraseFrameAll();
	bool SelectFrame(const POSITION& tPos);
	bool IsInFrame(const POSITION& tPos);

public:
	void OnEditDlg();
};

